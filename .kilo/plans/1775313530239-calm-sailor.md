# Dynamic Content Integration Implementation Plan

## Current State Analysis
- education.html: Currently displays static fallback content instead of dynamic Strapi data
- browse.html: Browse functionality implemented but may have JavaScript errors
- CMS routes: Three critical route files need conversion from Strapi v4 to v5 format

## Required Fixes

### 1. CMS Route Updates (Deploy Required)
**Files:** `womencypedia-cms/src/api/biography/routes/biography.js`, `collection.js`, `homepage.js`
**Action:** Convert manual v4 route definitions to `createCoreRouter()` factory functions
**Impact:** Resolves all API 404 errors preventing dynamic content loading

### 2. Frontend JavaScript Fixes
**Files:** `js/education-inline-1.js`, `js/browse-logic.js`, `js/featured-inline-1.js`, `featured.html`
**Actions:**
- Remove stray template literal syntax in education-inline-1.js
- Rewrite browse-logic.js for Strapi v5 compatibility and remove variable conflicts
- Fix CSS selector in featured-inline-1.js and add proper grid ID
**Impact:** Eliminates JavaScript errors causing content loading failures

### 3. Bootstrap Cleanup
**File:** `womencypedia-cms/src/index.ts`
**Action:** Remove debug code that's no longer needed in v5
**Impact:** Cleaner bootstrap process

## Implementation Steps
1. Apply CMS route fixes and redeploy Strapi CMS
2. Apply frontend JavaScript fixes and test locally
3. Verify API endpoints return 200 responses
4. Test dynamic content loading on education.html and browse.html
5. Confirm featured biographies load on featured.html

## Verification Criteria
- education.html loads education modules from Strapi API
- browse.html successfully fetches and displays biographies with filters
- featured.html populates editor's picks grid with dynamic data
- No JavaScript console errors on any pages
- All API calls return proper JSON responses

## Risk Assessment
- Medium: CMS redeploy required for route fixes
- Low: Frontend changes are isolated to JavaScript files
- Mitigation: Test locally before deployment, have rollback plan

## Dependencies
- Strapi CMS must be redeployed on Render after route changes
- Frontend changes can be deployed independently
- No database schema changes required