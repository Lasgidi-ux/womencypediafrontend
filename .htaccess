# Minimal .htaccess for GoDaddy
# If this still fails, try deleting .htaccess entirely

# Redirect HTTP to HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://yourdomain.com%{REQUEST_URI} [L,R=301]